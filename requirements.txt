# CPU-only PyTorch wheel index.
# NOTE: --extra-index-url only *adds* a source; pip still resolves to the highest
# version, which is the CUDA build on PyPI. For a guaranteed CPU wheel, pin the
# +cpu local version (below) or switch to --index-url.
--extra-index-url https://download.pytorch.org/whl/cpu

# Pinned upper bounds: unpinned installs are what broke the last build.
# numpy 2.0 removed np.trapz; matplotlib 3.11 removed boxplot(labels=).
numpy>=1.26,<3
pandas
matplotlib>=3.9
scipy
scikit-learn
torch
torchvision      # DL7 / P8 / P9 load torchvision.models
tensorflow-cpu   # TensorFlow Guide (tf1-tf8); -cpu variant keeps the CI install small

# NOTE: `transformers` is intentionally absent — DL7/P9 only mention it in
# comments and print statements; nothing imports it at execution time.

# Book build
jupyter-book<2
ipykernel
pydata-sphinx-theme
